# DEPLOY

When this command is given, you will:

1. first run the backup.md command, but ignoring the instructions about not pushing to Vercel
2. Push the deployment to the live Vercel production server